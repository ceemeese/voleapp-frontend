import type { App } from 'vue';

export default {
    install: (app: App) => {
        // Aquí podrías registrar componentes de forma global si quisieras
        console.log('Librería UI cargada correctamente');
    }
};